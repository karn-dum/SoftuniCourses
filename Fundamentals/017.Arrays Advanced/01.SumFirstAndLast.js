function solve(inp) {
    let num1 = Number(inp.slice(0,1));
    let num2 = Number(inp.slice(-1));
    console.log(num1 + num2);

    // let arr = inp.slice(1,3);
    // console.log(arr.join(' '));
    

}   


solve(['20', '30', '80', '40']);
solve(['5', '10']);